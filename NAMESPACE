
# import
import(
    wpp2019, data.table, bayesLife, coda, hett, bayesTFR
)
importFrom(car, logit)
importFrom("stats", "median", "na.omit", "optimise", "quantile",
            "rnorm", "splinefun")
importFrom("utils", "data")
  
#exportPattern("^[[:alpha:]]+")
useDynLib(bayesLifeHIV, .registration = TRUE)

# export functions
export(
	run.e0hiv.mcmc,
	e0hiv.predict,
	compute.hiv.loess,
	compute.hiv.residuals,
	e0hiv.scale.trajectories,
	hiv.countries.pred,
	hiv.countries.est,
	e0hiv.meta.ini,
	e0hiv.mcmc.sampling,
	generate.e0hiv.trajectory,
	e0hiv.get.dlcurves,
	using.bayesLifeHIV
)

# register S3 methods
